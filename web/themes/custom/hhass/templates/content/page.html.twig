<!DOCTYPE html>
<html lang="en">
  <head>
    <head-placeholder token="{{ placeholder_token }}">
    <title>{{ head_title }}</title>
    <css-placeholder token="{{ placeholder_token }}">
    <js-placeholder token="{{ placeholder_token }}">
  </head>
  <body class="font-sans antialiased bg-white text-gray-800">
    
    <!-- Header -->
    <header class="bg-white shadow-md">
      <div class="max-w-7xl mx-auto px-4 py-4 flex justify-between items-center">
        <div class="text-xl font-bold">HHASS</div>
        <nav class="space-x-4">
          <a href="#" class="text-gray-700 hover:text-blue-500">Home</a>
          <a href="#" class="text-gray-700 hover:text-blue-500">About</a>
          <a href="#" class="text-gray-700 hover:text-blue-500">Services</a>
          <a href="#contact" class="text-gray-700 hover:text-blue-500">Contact</a>
        </nav>
      </div>
    </header>

    <!-- Hero Section -->
    <section class="bg-blue-600 text-white py-20 text-center">
      <div class="max-w-3xl mx-auto">
        <h1 class="text-4xl font-bold mb-4">Your Digital Solution Partner</h1>
        <p class="mb-6">We build modern, fast, and scalable solutions for your business.</p>
        <a href="#services" class="bg-white text-blue-600 px-6 py-2 rounded font-semibold hover:bg-gray-100">Get Started</a>
      </div>
    </section>

    <!-- Services Section -->
    <section id="services" class="py-16 bg-gray-50">
      <div class="max-w-7xl mx-auto px-4">
        <h2 class="text-3xl font-semibold text-center mb-10">Our Services</h2>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
          <div class="bg-white p-6 rounded shadow text-center">
            <div class="text-blue-600 text-4xl mb-4">💻</div>
            <h3 class="font-bold text-lg">Web Development</h3>
            <p class="text-sm text-gray-600 mt-2">Modern and responsive websites tailored for your business.</p>
          </div>
          <div class="bg-white p-6 rounded shadow text-center">
            <div class="text-blue-600 text-4xl mb-4">📱</div>
            <h3 class="font-bold text-lg">App Design</h3>
            <p class="text-sm text-gray-600 mt-2">Elegant mobile and desktop applications with smooth UX.</p>
          </div>
          <div class="bg-white p-6 rounded shadow text-center">
            <div class="text-blue-600 text-4xl mb-4">🔒</div>
            <h3 class="font-bold text-lg">Security Consulting</h3>
            <p class="text-sm text-gray-600 mt-2">Protect your data and infrastructure with expert advice.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Contact Form -->
    <section id="contact" class="py-20 bg-white">
      <div class="max-w-2xl mx-auto px-4">
        <h2 class="text-3xl font-semibold text-center mb-6">Contact Us</h2>
        <form class="space-y-4">
          <input type="text" placeholder="Your Name" class="w-full px-4 py-2 border rounded" required>
          <input type="email" placeholder="Your Email" class="w-full px-4 py-2 border rounded" required>
          <textarea placeholder="Your Message" class="w-full px-4 py-2 border rounded" rows="4" required></textarea>
          <button type="submit" class="bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-700">Send</button>
        </form>
      </div>
    </section>

    <!-- Footer -->
    <footer class="bg-gray-900 text-white text-center py-6">
      <p>&copy; {{ "now"|date("Y") }} HHASS. All rights reserved.</p>
    </footer>

    <js-bottom-placeholder token="{{ placeholder_token }}">
  </body>
</html>

